[tool.poetry]
name = "my-prefect"
version = "0.1.0"
description = ""
authors = ["jz"]
readme = "README.md"
packages = [{include = "my_prefect", from = "src"}]

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
